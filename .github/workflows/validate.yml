name: Validate Tracking CSV
on:
  push:
    branches: [ "main" ]
  pull_request: {}
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run validator
        run: |
          python3 tools/validate_tracking.py
